spring:
   profiles:
      active:
        - dev
---
server:
  port: 8201

spring:
  profiles: dev
  application:
      name: mycloudconfig-test-client5

eureka:
 client:
  serviceUrl:
    defaultZone: http://admin:87654321@localhost:7301/eureka
  register-with-eureka: false

info:
  app.name: mycloudconfig-config-client-dev2
  company.name: enjoy2
---
server:
  port: 8102

spring:
  profiles: beta
  application:
      name: mycloudconfig-test-client5

eureka:
 client:
  serviceUrl:
    defaultZone: http://admin:87654321@localhost:7301/eureka
  register-with-eureka: false
  
info:
  app.name: mycloudconfig-config-client-beta2
  company.name: sg
